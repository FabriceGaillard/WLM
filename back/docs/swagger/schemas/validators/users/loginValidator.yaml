components:
  schemas:
    loginValidator:
      type: object
      properties:
        email:
          type: string
          format: email
          example: exemple@domain.com
        password:
          type: string
        remember:
          type: boolean
      required: [email, password]
