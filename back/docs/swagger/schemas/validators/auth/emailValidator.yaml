components:
  schemas:
    emailValidator:
      type: object
      properties:
        email:
          type: string
          format: email
          example: bot@example.com
      required: [email]
