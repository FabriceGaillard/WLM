components:
  schemas:
    updatePasswordUserValidator:
      type: object
      properties:
        params:
          type: object
          properties:
            userIds:
              type: array
              items:
                type: string
                format: uuid
          content:
            type: string
