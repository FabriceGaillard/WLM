/contacts/{id}:
  get:
    summary: Show a contact of authenticated a contact (a user)
    tags:
      - Contacts
    parameters:
      - name: id
        in: path
        required: true
        description: uuid of the contact (a user)
        schema:
          type: string
          format: uuid
    responses:
      '200':
        description: OK
        content:
          application/json:
            schema:
              type: object
      '404':
        description: Not Found
        content:
          application/json:
            schema:
              type: object
